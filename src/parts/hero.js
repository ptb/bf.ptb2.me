import React from "react"

const home = (a) => pug `
  div Home
`

const rates = (a) => pug `
  div Rates
`

const events = (a) => pug `
  div Events
`

const gallery = (a) => pug `
  div Gallery
`

const djs = (a) => pug `
  div The DJs
`

const music = (a) => pug `
  div Music
`

const equipment = (a) => pug `
  div Equipment
`

const press = (a) => pug `
  div Press
`

export {
  home,
  rates,
  events,
  gallery,
  djs,
  music,
  equipment,
  press
}
